# 04 · 元件

## 索引

| 家族 | 元件 |
| --- | --- |
| [動作](actions.md) | 按鈕、圖示按鈕、底部動作列、選單項 |
| [點名](roll-call.md) | 名單列、Chip、計分區、分段控制、同步指示 |
| [輸入](inputs.md) | 輸入框、多行輸入、搜尋、房號輸入、名單解析預覽 |
| [疊層](overlays.md) | 底部面板、確認對話框、Toast |
| [回饋](feedback.md) | 空狀態、載入骨架、提示框、進度條 |
| [表面](surfaces.md) | 頂欄、卡片、房號大字、QR 卡片、標籤 |

## 規格模板

每個元件都用同一套欄位描述。新增元件時照抄這個結構。

```
### 元件名 `.class`

**用途** — 一句話。

**不要用在** — 最容易被誤用的場景。

**解剖** — 有名字的組成部位。

**變體 / 尺寸** — 有哪些，各自什麼時候用。

**狀態** — 預設 / hover / 按下 / 焦點 / 停用 / 載入 / 錯誤，缺哪個要說明為什麼。

**內容規則** — 字數上限、超長怎麼處理、空值怎麼辦。

**無障礙契約** — role、aria、鍵盤行為、焦點。

**實作** — 檔案位置。
```

> **不要用在**這一欄比其他欄位都重要。多數設計系統壞掉不是因為缺元件，
> 是因為某個元件被拿去做它不該做的事。

## 通則

1. 元件不得自帶外部邊距。間距由父層的 `.stack` / `.row` 用 `gap` 決定。
2. 元件不得寫死顏色、字級、間距，一律用 token。
3. 每個可互動元件都要有無障礙名稱。
4. 有 hover 態的一定要有 `:focus-visible` 態。
5. 新增變體之前先問：這是新變體，還是既有元件用錯地方了？

# 元件 · 表面

### 頂欄 `.topbar`

**用途** — 我的身分、空間身分、同步狀態、以及一個全域動作。

**解剖**

```
.topbar-inner
├─ 返回（icon-btn 24）
├─ .topbar-title
│   ├─ .topbar-name   空間名稱，單行截斷
│   └─ .topbar-sub    .tag 身分 ＋ 代碼 或 .topbar-count ＋ 同步指示
└─ 更多（icon-btn）
```

**規則**

- `position: sticky`、`--z-sticky`、上方留 `env(safe-area-inset-top)`。
- 副標平常顯示**代碼**；空間關閉時換成「已關閉」（`.topbar-count`）——關閉是全域狀態，不能只靠一條會捲走的橫幅。
- **身分標籤（主揪／協助者）排在代碼前面**（2026-09 從「更多」面板的身分列搬來）。順序是身分 → 空間 → 連線：「我能不能改」決定這個畫面上哪些事做得動（改名單、結束這一輪只有主揪能做），那是進空間第一眼就該知道的事，不該要先點開「更多」面板。
- **這一列擠不下時，先收同步狀態的文字。** 三格之中標籤與代碼不縮（一個是「我能不能改」，一個是要唸給別人聽的東西，截一半都沒有用），`.sync-text` 才是那個讓步的——圓點與顏色留著，完整說法在 `aria-label`／`title` 裡。
- **篩選與搜尋都住在頂欄裡**，所以跟著黏住。未到人數就在篩選那排（「未到 N」），不需要另一套「捲出畫面時接手」的機關。
- **標題可點，點了回到名單頂端**（`aria-label` 用 `backToTop`）。200 人的名單捲到底之後，要回到搜尋框得往上滑 17 個螢幕，而 `overscroll-behavior-y: none` 連「用力甩」都擋掉了。這是行動裝置的既有慣例，不必再教。
- 頂欄只留一顆圖示動作（更多）。分享 2026-09 收進「更多」並改名「邀請點名」：整場只用一次的東西不佔頂欄，頂欄的位置留給一直都要按得到的。代碼本來就一直印在頂欄那一行上。

**實作** — `src/ui/Room.tsx`

---

### 卡片 `.card`

**用途** — 把一組相關的控制項圈在一起（例如首頁的「加入空間」）。

**不要用在** — 名單項目。列表用 `.member` 或 `.recent-item`，它們有各自的觸控規則。

**規則** — `--surface` 底、`--rule` 邊框、`--r-3` 圓角、`--el-1` 陰影、`--sp-4` 內距。不巢狀。

**實作** — `src/styles.css`

---

### 代碼大字 `.code-display`

**用途** — 在「更多 › 邀請點名 › 代碼」那一頁把代碼顯示到隔著一支手臂也唸得出來。

> **邀請點名是「更多」的第一列**（2026-09：從頂欄那顆圖示鍵搬進來，並從「分享」
> 改名——「分享」那兩個字在手機上專指「叫出系統分享單」。首頁每一列右邊那顆
> 「更多」打開的是同一份選單，見 04-components/overlays.md）。三種方式各自
> 一列、各自一頁，順序是**代碼 → 連結 → 二維碼**：代碼是隔著車門喊得出去的，
> 連結是貼進 LINE 群的，二維碼要對方拿起手機對著你的螢幕——愈往下愈需要兩個
> 人站在一起。一頁只放一種，`.code-display` 才有整頁的寬度可用。
>
> **邀請頁底下還有一區「現在在這個空間裡」**（雲端模式、且 presence 真的通了才
> 顯示）：它回答的是「代碼發出去之後有沒有人真的進來」，跟發代碼是同一件事的
> 兩半。規則見 05-patterns §5.9.1——重點是 **REST 通了不代表 presence 通了，
> 沒把握就整區不顯示**，絕不能在別人明明已經進來時說「目前只有你」。
>
> **單機模式下整頁換掉。** `connection === 'local-only'` 時三種方式一個都不列
> （沒有 `.code-display`、沒有 `.qr-card`、沒有連結按鈕），改成一塊
> `.note-warn`：「這個空間只有你看得到」＋別人會看到什麼。發出去的代碼在這個
> 模式下對任何人都沒有用——五個同工會在車門口看到「找不到這個代碼。請確認有
> 沒有打錯」，然後以為是自己打錯而重試三次。見 01-foundations §1.2 原則 5 的
> 第 1 條。**但同一份選單裡的「匯出結果」照樣在**：印一張紙、存一份 CSV 或 PDF
> 不需要任何連線，把它一起藏掉等於單機使用者連名單都帶不走。

**規則** — 等寬、`--fs-7`、字距 `.18em`、置中。字距造成的視覺偏移用左右不等內距補償。

**內容規則** — 永遠大寫，永遠不加分隔符（`K7F2QM` 不寫成 `K7F-2QM`）——使用者會照著打，多一個符號就多一次打錯。

**實作** — `src/ui/Sheets.tsx`

---

### QR 卡片 `.qr-card`

**用途** — 讓協助者掃碼加入。

**規則**

- **一律白底黑碼，即使深色模式。** 掃描器要的是對比。
- 這是全系統唯一允許寫死 `#fff` 的地方，例外寫在 `src/styles.test.ts`。
- 最大寬 240px（更大不會更好掃，只會佔掉面板空間）。
- 產生失敗時安靜降級——代碼與連結仍可用，不顯示錯誤。

**無障礙契約** — `<img>` 的 `alt` 要包含代碼，讓看不到 QR 的人也拿得到資訊。

**實作** — `src/ui/Sheets.tsx`（`QrCard`）

---

### 標籤 `.tag`

**用途** — 標示身分或分類，例如「主揪」／「協助者」。

**不要用在** — 名單成員的附加資訊（那是 chip）。tag 是身分，chip 是屬性。

**變體** — `.tag`（中性）、`.tag-owner`（`--accent-soft` 底、`--accent` 字）。

**規則** — 藥丸形、`--fs-2`、不可點擊。要可點就不是 tag 了。

**實作** — `src/ui/Home.tsx`
